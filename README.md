# 26INF1103P5-4
INF1103 Project

Before you start working on your own section, please do the following:
1) ```git pull``` to make sure you have the latest version of the repository

3) ```git checkout``` -b <your_section> for <section>, input your assigned role.
   For example, if I am doing IO Manager, I'll put ```git checkout -b io_manager```. This will be the branch you're working on

5) after this, you can ```git add, commit and push``` as you would normally

6) when you're finished with your work (like at the END you have NOTHING ELSE TO ADD), create a pull request on github.com
